// Fichier stub pour mobile (Android/iOS) - ces fonctions ne sont pas utilisées sur mobile
// mais doivent être définies pour que le code compile

String? getWebHostname() {
  // Sur mobile, cette fonction n'est jamais appelée (kIsWeb est false)
  // Mais elle doit être définie pour la compilation
  return null;
}

String getWebProtocol() {
  // Sur mobile, cette fonction n'est jamais appelée (kIsWeb est false)
  // Mais elle doit être définie pour la compilation
  return 'http:';
}

