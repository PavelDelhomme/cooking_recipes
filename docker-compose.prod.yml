services:
  cookingrecipe-api:
    image: paveldelhomme/cookingrecipe-api:latest
    container_name: cookingrecipe-api
    restart: unless-stopped
    expose:
      - "7272"
    volumes:
      - cooking_recipes_backend_data:/app/data
    environment:
      - PORT=7272
      - HOST=0.0.0.0
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET:-change-this-secret-in-production}
    networks:
      - cooking_recipes_network
      - web
    labels:
      - "com.docker.compose.project=cooking_recipes"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:7272/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  cookingrecipe-frontend:
    image: paveldelhomme/cookingrecipe-frontend:latest
    container_name: cookingrecipe-frontend
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - cooking_recipes_frontend_build:/app/build
    environment:
      - API_URL=https://api.cookingrecipe.delhomme.ovh
      - FLUTTER_WEB_USE_SKIA=true
    depends_on:
      - cookingrecipe-api
    networks:
      - cooking_recipes_network
      - web
    labels:
      - "com.docker.compose.project=cooking_recipes"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  cooking_recipes_backend_data:
    driver: local
    name: cooking_recipes_backend_data
  cooking_recipes_frontend_build:
    driver: local
    name: cooking_recipes_frontend_build

networks:
  cooking_recipes_network:
    driver: bridge
    name: cooking_recipes_network
  web:
    external: true
    name: web
